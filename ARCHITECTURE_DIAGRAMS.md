# Architecture & Data Flow Diagrams

## 

```
┌─────────────────────────────────────────────────────────────────┐
│                         AFRIMART SYSTEM                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐                    ┌──────────────┐           │
│  │   SELLER     │                    │   CUSTOMER   │           │
│  └──────┬───────┘                    └──────┬───────┘           │
│         │                                   │                   │
│    SELLER DASHBOARD                    BROWSE & BUY             │
│    • Add Product                       • View Product            │
│    • Upload Images                     • See All Images          │
│    • Manage Orders                     • Add to Cart             │
│         │                                   │                   │
│         └───────────────┬───────────────────┘                   │
│                         │                                       │
│                  ┌──────▼──────┐                                │
│                  │   PRODUCT   │                                │
│                  │   MODEL     │                                │
│                  └──────┬──────┘                                │
│                         │                                       │
│        ┌────────────────┼────────────────┐                      │
│        │                │                │                      │
│    ┌───▼────┐   ┌──────▼──────┐   ┌────▼────┐                  │
│    │ Product │   │ ProductImage │   │ Category │                │
│    │ Details │   │  (Multiple)  │   │          │                │
│    └────┬────┘   └──────┬───────┘   └────┬────┘                │
│         │                │                 │                   │
│         │        ┌───────▼─────────┐      │                   │
│         │        │  RAILWAY S3      │      │                   │
│         │        │  BUCKET STORAGE  │      │                   │
│         │        └──────────────────┘      │                   │
│         │                                  │                   │
│         └──────────────┬─────────────────┬─┘                   │
│                        │                 │                      │
│                  ┌─────▼────────────────▼─┐                     │
│                  │    DJANGO TEMPLATES     │                    │
│                  │  & BOOTSTRAP UI         │                    │
│                  └─────────────────────────┘                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow Diagram

```
SELLER JOURNEY
═════════════════════════════════════════════════════════════════

1. SELLER LOGIN
   ┌─────────────┐
   │ Login Page  │
   └──────┬──────┘
          │
          ▼
   ┌─────────────────────┐
   │ Seller Dashboard    │
   │ (Stats & Overview)  │
   └──────┬──────────────┘
          │
          ├─→ Create Product ─┐
          │                   │
          ├─→ Edit Product ─┐ │
          │                │ │
          └─→ View Orders  │ │
                           │ │
2. PRODUCT MANAGEMENT    ▼ ▼
   ┌────────────────────────────┐
   │   Add Product Form         │
   │ • Name, Description, Price │
   │ • Category, Stock, Discount│
   └─────────────┬──────────────┘
                 │
                 ▼
   ┌────────────────────────────┐
   │  Save to Database          │
   │  (Product Model)           │
   └─────────────┬──────────────┘
                 │
                 ▼
   ┌────────────────────────────┐
   │  Upload Images Page        │
   │  (Single or Multiple)      │
   └─────────────┬──────────────┘
                 │
                 ▼
   ┌────────────────────────────┐
   │  Images Processing         │
   │  • Validate file type      │
   │  • Save to Railway S3      │
   │  • Create ProductImage     │
   │    records in DB           │
   └─────────────┬──────────────┘
                 │
                 ▼
   ┌────────────────────────────┐
   │  Set Primary Image         │
   │  (Featured for listings)   │
   └─────────────┬──────────────┘
                 │
                 ▼
   ┌────────────────────────────┐
   │  Product Live!             │
   │  • On product listings     │
   │  • In customer search      │
   │  • Ready for purchase      │
   └────────────────────────────┘


CUSTOMER JOURNEY
═════════════════════════════════════════════════════════════════

1. BROWSE
   ┌──────────────────┐
   │  Product Listing │ ← Shows primary image + count of images
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Click Product        │
   └────────┬─────────────┘
            │
2. VIEW DETAILS
            ▼
   ┌──────────────────────────────┐
   │ Product Detail Page          │
   │ • Carousel with all images   │
   │ • Thumbnail strip            │
   │ • Primary image highlighted  │
   └────────┬─────────────────────┘
            │
            ▼
   ┌──────────────────────────────┐
   │ Browse through gallery       │
   │ (All ProductImage records)   │
   └────────┬─────────────────────┘
            │
3. PURCHASE
            ▼
   ┌──────────────────────────────┐
   │ Add to Cart                  │
   │ (Images included in order)   │
   └────────┬─────────────────────┘
            │
            ▼
   ┌──────────────────────────────┐
   │ Order Complete               │
   │ + Seller sees order          │
   │ + Seller tracks shipment     │
   └──────────────────────────────┘
```

## 📊 Database Schema

```
PRODUCTS TABLE
═════════════════════════════════════════════════════════════════
┌─────────────────────────────────────────────────────────────┐
│ id (PK)     │ name         │ price   │ stock   │ seller_id  │
│ description │ discount_%   │ is_fea… │ created │ updated    │
│ image (old) │ cloudinary_… │ categ… │         │            │
└─────────────────────────────────────────────────────────────┘
                          ▲
                          │
                          │ 1:Many
                          │
PRODUCT_IMAGE TABLE
┌─────────────────────────────────────────────────────────────┐
│ id (PK)     │ product_id (FK)  │ image         │ is_primary │
│ order       │ uploaded_at      │ (S3 URL)      │            │
└─────────────────────────────────────────────────────────────┘
                          │
                          │ Stored in
                          ▼
                   RAILWAY S3 BUCKET
                   ┌─────────────────┐
                   │  Images stored  │
                   │  as files       │
                   └─────────────────┘
```

## 🔐 Access Control Flow

```
AUTHENTICATION & AUTHORIZATION
═════════════════════════════════════════════════════════════════

Request to /seller/dashboard/
         │
         ▼
┌─────────────────────┐
│ @login_required     │
│ decorator check     │
└────────┬────────────┘
         │
         ├─ NOT LOGGED IN ──→ Redirect to /login/
         │
         └─ LOGGED IN
                │
                ▼
         ┌──────────────────────┐
         │ View executes:       │
         │ seller_dashboard()   │
         └────────┬─────────────┘
                  │
                  ▼
         ┌──────────────────────────────┐
         │ Get user's products:         │
         │ Product.objects.filter(      │
         │   seller=request.user        │
         │ )                            │
         └────────┬─────────────────────┘
                  │
                  ▼
         ┌──────────────────────────────┐
         │ Only seller's data shown     │
         │ Other sellers' data hidden   │
         └──────────────────────────────┘

EDIT PRODUCT FLOW
─────────────────────────────────────────────────────────────────

Request: /seller/product/{id}/edit/
         │
         ▼
┌─────────────────────┐
│ Get product         │
│ by ID               │
└────────┬────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ Check ownership:                │
│ if product.seller == user:      │
└────────┬──────────┬─────────────┘
         │          │
    YES  │          │  NO
         ▼          ▼
      ALLOWED    ERROR 403
                 "Cannot edit"
```

## 📁 File Structure

```
AFRIMART PROJECT
═════════════════════════════════════════════════════════════════

afrimart/
├── requirements.txt ......................... Dependencies
├── QUICK_START.md .......................... Quick setup
├── SELLER_DASHBOARD_GUIDE.md ............... Full guide
├── IMPLEMENTATION_CHECKLIST.md ............. Deployment
├── FRONTEND_INTEGRATION_GUIDE.md ........... Code examples
├── README_IMPLEMENTATION.md ................ Summary
│
└── src/
    ├── manage.py
    ├── db.sqlite3 (or DATABASE_URL)
    │
    ├── app/
    │   └── settings.py ← S3 configuration
    │
    ├── main/ ◄────────────── MAIN APP
    │   ├── models.py ◄────────── Updated: ProductImage model
    │   ├── forms.py ◄────────── Updated: Product forms
    │   ├── views.py ◄────────── Updated: 10 seller views
    │   ├── urls.py ◄────────── Updated: Seller routes
    │   ├── admin.py ◄────────── Updated: Image admin
    │   │
    │   ├── migrations/
    │   │   └── 0004_productimage.py ◄────── NEW
    │   │
    │   └── management/
    │       └── commands/ (existing)
    │
    ├── escrow/
    │   └── ... (existing)
    │
    └── templates/
        ├── base.html
        ├── main/
        │   ├── home.html
        │   ├── product_detail.html
        │   └── seller/ ◄────────── NEW DIRECTORY
        │       ├── dashboard.html ◄────── NEW
        │       ├── products.html ◄────────── NEW
        │       ├── add_product.html ◄────────── NEW
        │       ├── edit_product.html ◄────────── NEW
        │       ├── upload_images.html ◄────────── NEW
        │       ├── delete_product.html ◄────────── NEW
        │       ├── delete_image.html ◄────────── NEW
        │       ├── orders.html ◄────────── NEW
        │       └── order_detail.html ◄────────── NEW
        │
        └── emails/
            └── ... (existing)
```

## 🌊 Request/Response Flow

```
USER UPLOADS MULTIPLE IMAGES
═════════════════════════════════════════════════════════════════

User selects 5 images → POST to upload_product_images()
         │
         ▼
┌─────────────────────────────────────┐
│ View receives request               │
│ • Check seller ownership            │
│ • Validate CSRF token              │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│ For each uploaded file:             │
│  1. Validate file type              │
│  2. Create ProductImage object      │
│  3. Save image to S3 bucket         │
│  4. Store reference in database     │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│ S3 Bucket                           │
│ ├─ image_001.jpg                    │
│ ├─ image_002.jpg                    │
│ ├─ image_003.jpg                    │
│ ├─ image_004.jpg                    │
│ └─ image_005.jpg                    │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│ Database ProductImage records:      │
│ ├─ id:1, product_id:5, is_primary:1 │
│ ├─ id:2, product_id:5, is_primary:0 │
│ ├─ id:3, product_id:5, is_primary:0 │
│ ├─ id:4, product_id:5, is_primary:0 │
│ └─ id:5, product_id:5, is_primary:0 │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│ Response: "5 images uploaded!"      │
│ Redirect to: /seller/product/5/edit/│
└─────────────────────────────────────┘


CUSTOMER VIEWS PRODUCT
═════════════════════════════════════════════════════════════════

User clicks product link
         │
         ▼
/product/{id}/
         │
         ▼
┌─────────────────────────────────────┐
│ product_detail view                 │
│ • Get Product object                │
│ • Get all ProductImage objects      │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│ Database queries:                   │
│ SELECT * FROM product WHERE id=5    │
│ SELECT * FROM productimage          │
│   WHERE product_id=5                │
│   ORDER BY is_primary DESC, order ASC
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│ Context data passed to template:    │
│ • product: Product object           │
│ • images: QuerySet[ProductImage]    │
│ • primary_img_url: string           │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│ Template renders:                   │
│ • Primary image in main view        │
│ • All images in carousel            │
│ • Thumbnails for quick select       │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│ HTML response to browser            │
│ Browser loads images from S3        │
│ User sees complete gallery          │
└─────────────────────────────────────┘
```

## 🚀 Deployment Pipeline

```
LOCAL → GIT → RAILWAY → LIVE
═════════════════════════════════════════════════════════════════

1. LOCAL DEVELOPMENT
   ┌────────────────────┐
   │ Make changes       │
   │ • Add models      │
   │ • Add forms       │
   │ • Add views       │
   │ • Add templates   │
   └────────┬───────────┘
            │
            ▼
   ┌────────────────────┐
   │ Test locally       │
   │ • python manage.py │
   │   runserver        │
   └────────┬───────────┘
            │
            ▼
2. VERSION CONTROL
   ┌────────────────────┐
   │ git add .          │
   │ git commit -m "..." │
   │ git push           │
   └────────┬───────────┘
            │
            ▼
   ┌────────────────────────┐
   │ GitHub Repository      │
   │ (Code stored)          │
   └────────┬───────────────┘
            │
            ▼
3. RAILWAY DEPLOYMENT
   ┌──────────────────────────┐
   │ Webhook triggered        │
   │ • Pull code from GitHub  │
   │ • Install dependencies   │
   │ • Run migrations         │
   │ • Collect static files   │
   │ • Restart server         │
   └────────┬─────────────────┘
            │
            ▼
   ┌──────────────────────────┐
   │ Environment Variables    │
   │ • USE_BUCKET=true       │
   │ • AWS credentials       │
   │ • Database URL          │
   └────────┬─────────────────┘
            │
            ▼
   ┌──────────────────────────┐
   │ S3 Bucket Connected      │
   │ • Images stored          │
   │ • URLs configured        │
   └────────┬─────────────────┘
            │
            ▼
4. LIVE PRODUCTION
   ┌──────────────────────────┐
   │ https://afrimart.app     │
   │ • Sellers manage products│
   │ • Customers browse store │
   │ • Images load from S3    │
   └──────────────────────────┘
```

---

**Diagrams Created**: 8  
**Architecture Components**: 12  
**Data Flows Documented**: 6  
**Deployment Stages**: 4  

All diagrams show the complete system flow from seller to customer to database to S3 storage.
